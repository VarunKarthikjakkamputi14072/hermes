# Point the dashboard at a running order-api. When unset, the UI runs in
# self-contained DEMO mode (a client-side simulator) so the queue visualization
# is alive without booting Kafka + Postgres.
NEXT_PUBLIC_HERMES_API_BASE_URL=http://localhost:8080
